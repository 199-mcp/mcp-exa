{
  "version": 2,
  "builds": [
    { "src": "api/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/mcp(?:/.*)?", "dest": "/api/mcp.ts" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "env": {
    "MCP_TOOLS": "web_search_exa"
  }
}
